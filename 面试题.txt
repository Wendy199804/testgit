1. 数组去重
 排序后一组循环
 双重循环
 将数组的值变成对象的key，在循环输出对象的 key
 利用 set 进行去重...(new Set(arr))
 循环推入一个新数组，推入前判断新数组中是否存在
 数组的 filter 返回 数组中的第一个索引==当前索引值

2.$(document).ready和window.onload的区别
 前者将 dom 渲染完成后就可以执行，后者要等其他所有静态文件加载完毕才可以执行

3、js 无法加载
 

4、原生 ajax

var ajax = new XMLHttpRequest();
ajax.open('get','getStar.php?starName='+name);
ajax.send();
ajax.onreadystatechange = function () {
   if (ajax.readyState==4 &&ajax.status==200) {
　　　　console.log(ajax.responseText);
  　　}
}

5.不定宽高水平垂直居中的方法
 弹性布局水平垂直居中
 绝对定位水平垂直 50%，利用 transition 进行自身偏移
 父元素设置display: table-cell，vertical-align: middle  子元素设置vertical-align: middle

6、map，数组，对象互相转换
https://blog.csdn.net/c__dreamer/article/details/82183130#Map%E8%BD%AC%E4%B8%BA%E6%95%B0%E7%BB%84

